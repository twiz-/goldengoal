<%= simple_form_for([@user, @player]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :player_name %>
  </div>
  
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
